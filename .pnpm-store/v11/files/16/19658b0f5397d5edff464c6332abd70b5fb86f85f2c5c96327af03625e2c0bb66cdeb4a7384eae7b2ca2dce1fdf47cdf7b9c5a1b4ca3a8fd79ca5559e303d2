export const provider = "none";
export async function listPageCollections(_event) {
  return [];
}
export function queryPages(_event, collection) {
  throw new Error(`No content module is installed, so collection "${collection}" cannot be queried.`);
}
