import { Renderer } from '@takumi-rs/wasm/no-bundler';
import { extractResourceUrls } from './resource-urls.js';
declare const _default: {
    initWasmPromise: Promise<import("@takumi-rs/wasm").InitOutput>;
    Renderer: typeof Renderer;
    extractResourceUrls: typeof extractResourceUrls;
};
export default _default;
